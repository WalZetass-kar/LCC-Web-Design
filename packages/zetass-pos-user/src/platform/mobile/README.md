# Mobile Platform

Capacitor Android/iOS runtime files live in `android/`, `ios/`, and
`capacitor.config.ts`.

Mobile uses the responsive React UI, Capacitor plugins, and the mobile API/local
storage bridge already present in the renderer utilities.
