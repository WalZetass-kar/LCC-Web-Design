# Desktop Platform

Electron runtime and desktop packaging live in `src/main`, `dist-electron`, and
the Electron Builder config in `package.json`.

Desktop builds package SQLite, migrations, production config, and app icons as
resources.
