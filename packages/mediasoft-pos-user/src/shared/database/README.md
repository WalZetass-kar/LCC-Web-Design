# Shared Database

Desktop SQLite code currently lives in `src/database`.

Mobile storage logic currently lives in `src/renderer/utils/mobileApi.ts`.
This directory marks the future shared boundary for database adapters.
