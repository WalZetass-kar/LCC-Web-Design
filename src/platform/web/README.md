# Web Platform

The Vite web build in `dist/` is shared by Electron and Capacitor.

Standalone web hosting is not the primary target, but this folder marks the
platform boundary if a web shell is needed later.
